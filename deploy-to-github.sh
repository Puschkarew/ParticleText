#!/bin/bash

# Скрипт для создания репозитория на GitHub и загрузки кода
# Убедитесь, что вы авторизованы: gh auth login

REPO_NAME="physx-title"

echo "Создание публичного репозитория на GitHub..."
gh repo create $REPO_NAME --public --source=. --remote=origin --push

if [ $? -eq 0 ]; then
    echo "✅ Репозиторий успешно создан и код загружен!"
    echo "🌐 Ваш проект доступен по адресу:"
    gh repo view --web
else
    echo "❌ Ошибка при создании репозитория"
    echo "Убедитесь, что вы авторизованы: gh auth login"
    exit 1
fi
